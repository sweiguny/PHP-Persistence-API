<?php

use PPA\dbal\DriverManager;

require_once __DIR__ . "/../../vendor/autoload.php";

define("PPA_BOOTSTRAP_PATH", __DIR__);

// build up the fixtures for each database system

//$driverName = $GLOBALS["driver"];
//$username   = $GLOBALS["username"];
//$password   = $GLOBALS["password"];
//$database   = $GLOBALS["database"];
//$hostname   = $GLOBALS["hostname"];
//$port       = isset($GLOBALS["port"]) ?: null;
//
//
//$connection = DriverManager::getConnection($driverName, [], $username, $password, $hostname, $database, $port);
//$connection->connect();

?>